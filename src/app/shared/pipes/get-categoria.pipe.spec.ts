import { GetCategoriaPipe } from './get-categoria.pipe';

describe('GetCategoriaPipe', () => {
  it('create an instance', () => {
    const pipe = new GetCategoriaPipe();
    expect(pipe).toBeTruthy();
  });
});
